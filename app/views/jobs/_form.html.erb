<%= form_for @jobs do |f| %>

  <!-- <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.label :fonticon %>
  <%= f.text_field :fonticon %> -->


  <%= f.submit (@job.new_record? ? "Create" : "Update") %>

<% end %>
