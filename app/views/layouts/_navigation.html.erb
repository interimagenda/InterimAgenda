<nav id="mainNav" class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span><i class="fa fa-bars"></i>
      </button>
      <a class="navbar-brand page-scroll" href="#page-top">SOME_NAME_HERE</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to conversations_path do %>
            <p><i class="fa fa-envelope text-center" aria-hidden="true"></i>
            messages</p>
          <% end %></li>
          <li><%= link_to edit_user_registration_path do %>
            <p><i class="fa fa-user text-center" aria-hidden="true"></i>
            account</p>
          <% end %></li>

          <% else %>
          <li><%= link_to new_user_session_path do %>
            <p><i class="fa fa-sign-in text-center" aria-hidden="true"></i>
            login</p>
          <% end %></li>
          <li><%= link_to new_user_registration_path do %>
            <p><i class="fa fa-user" aria-hidden="true"></i>
            register</p>
          <% end %>
          </li>
        <% end %>
        <% if user_signed_in? %>
          <li><%= link_to destroy_user_session_path, method: :delete do %>
            <p><i class="fa fa-times text-center" aria-hidden="true"></i>
            log-out</p>
          <% end %></li>

        <% end %>

        <!-- freelancer logged in -->

        <% if user_signed_in? && current_user.type === 'Freelancer'%>
          <li><%= link_to employers_path do %>
            <p><i class="fa fa-building-o text-center" aria-hidden="true"></i> employers</p>
          <% end %></li>
          <li><%= link_to jobs_path do %>
            <p><i class="fa fa-briefcase text-center" aria-hidden="true"></i> jobs</p>
          <% end %>
          </li>
        <% end %>

          <!-- employer logged in -->

        <% if user_signed_in? && current_user.type === 'Employer'%>
          <li><%= link_to freelancers_path do %>
            <p><i class="fa fa-building-o text-center" aria-hidden="true"></i><br>freelancers</p>
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
